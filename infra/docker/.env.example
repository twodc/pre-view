# ===========================================
# PreView EC2 배포용 환경변수 템플릿
# ===========================================
# 이 파일을 .env로 복사한 후 실제 값을 입력하세요
# cp .env.example .env

# MySQL 설정
MYSQL_ROOT_PASSWORD=your_root_password_here
MYSQL_DATABASE=preview
MYSQL_USERNAME=preview_user
MYSQL_PASSWORD=your_mysql_password_here

# JWT 설정 (최소 256비트 = 32자 이상의 랜덤 문자열)
# 생성 명령어: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_key_here_at_least_32_characters

# Google OAuth2 설정
# Google Cloud Console에서 발급: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# OAuth2 리다이렉트 URI (EC2 퍼블릭 IP 또는 도메인)
# 예: http://3.35.xxx.xxx/oauth/callback 또는 http://your-domain.com/oauth/callback
OAUTH2_REDIRECT_URI=http://YOUR_EC2_PUBLIC_IP/oauth/callback

# Groq AI API 키
# Groq Console에서 발급: https://console.groq.com/keys
GROQ_API_KEY=gsk_your_groq_api_key_here

# 프론트엔드 API URL (Nginx 프록시 사용 시 빈 값 또는 상대 경로)
VITE_API_BASE_URL=/api/v1

# 쿠키 설정 (HTTP 환경)
COOKIE_SECURE=false
COOKIE_SAME_SITE=Lax

# ===========================================
# HTTPS 환경에서는 아래 설정 사용
# ===========================================
# COOKIE_SECURE=true
# COOKIE_SAME_SITE=None
